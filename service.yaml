apiVersion: v1
kind: Service

metadata:
  name: quickdeploy-service

spec:
  type: NodePort

  selector:
    app: quickdeploy

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007